project Default is

   for Source_Dirs use ("src", "src/avtas", "src/avtas/lmcp", "src/afrl", "src/afrl/cmasi");
   for Object_Dir use "obj";
   for Main use ("main.adb");

   package Builder is
      for Switches ("ada") use ("-j0");
   end Builder;

end Default;
